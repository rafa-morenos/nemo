# Nemo — Design System da Daki

Design system multiplataforma. **Uma fonte de tokens (Figma) → web (shadcn/ui + Tailwind), React Native e Flutter.** Marca azul ("mar azulão"), brand = `#0069ff`.

## Onde as coisas estão
- **Raiz:** `/Users/daki/Desktop/nemo` (clone deste repo no GitHub — `rafa-morenos/nemo`). Nota: este clone é um snapshot mais cru que versões anteriores do projeto: não tem `.husky`/`commitlint`/`.changeset`/CI, e `node_modules`/`build/*` (gitignored) precisam ser gerados localmente (`npm install` + `npm run build:tokens`).
- `tokens/` — fonte DTCG: `core.json` (primitivos + layout + tipografia), `semantic.light.json` / `semantic.dark.json` (árvore de alias). `figma-export.tokens.json` = export bruto do Tokens Studio.
- `build/` — `build.mjs` (Style Dictionary), `import-figma-tokens.mjs` (Tokens Studio → tokens/), saídas em `build/{web,ts,rn,flutter}` + `build/manifest.json`.
- `packages/web` — Storybook + componentes React (shadcn) + `tailwind.preset.js`. `src/icons/` — lib de ícones `icons-DakiApp` (assets reais do Figma, ver Status).
- `packages/react-native`, `packages/flutter` — espelhos (mesmos tokens gerados).
- `preview/nemo-artifact.html` + `build-artifact.mjs` — showcase estático (Artifact).

## Comandos
```bash
# rodar Storybook (porta 6007) — use a task do launch.json:
#   preview_start({ name: "storybook" })   ← preferir isto
cd /Users/daki/Claude/Nemo-project && npm run build:tokens          # regenera tokens → 4 plataformas
node build/import-figma-tokens.mjs                                  # reimporta o export do Figma
cd packages/web && npm run build-storybook                          # storybook-static/
node preview/build-artifact.mjs                                     # regenera o artifact
```
Storybook: **sempre** via `preview_start({name:"storybook"})` (nunca `npm` direto pra server). Ao adicionar deps novas, **reinicie** o server (preview_stop + preview_start) pra reotimizar.

## Como os tokens funcionam (importante)
- Sistema Material-3 em camadas: **Primitivos** (rampas tonais 0–100) → **Color Palette Light/Dark** → **Alias** (produto: `surface/text/border/icon/interactive/background`, cada um com `neutral/accent/semantic/medal`).
- A fonte de tokens é **100% o export do Figma** — não inventar tokens. Pra atualizar: exporta do Tokens Studio → substitui `tokens/figma-export.tokens.json` → `node build/import-figma-tokens.mjs && npm run build:tokens`.
- `semantic.light.json`/`semantic.dark.json` são **saída gerada**, não fonte — `import-figma-tokens.mjs` reconstrói os dois inteiros a partir do set `Alias colors /Value` do Figma a cada run. Editá-los na mão não sobrevive ao próximo import.
- Cada cor semântica (Critical/Success/Warning/Info) tem 8 roles na Color Palette do Figma, mas **só 4 chegam no Alias** (os únicos que `Alias colors /Value` referencia): `<Hue>` e `On <Hue> Container` → nossos `icon/border.semantic.*` (tom forte, junto com `Container`) e `text.semantic.*` (par "soft", o que o `filled` usa). O role `On <Hue>` — o texto branco correto pra usar quando `<Hue>` é o *fundo* (não ícone/borda) — nunca foi promovido pro Alias, só existe dentro da Color Palette.
- **Como o `solid` do `Badge`/`destructive` do `Button` resolvem isso sem esse role**: em vez de pinar num primitivo (já tentamos isso com um token `*-fixed` hand-authored e revertemos — ver histórico do PR), a solução adotada é parear o tom forte que já existe (`icon-semantic-critical`/`success`/`warning`, que inverte tom entre os temas) com `text-neutral-inverted` como texto — esse alias *também* inverte, na mesma direção (quase branco no claro, quase preto no escuro), então o contraste se mantém nos dois temas usando só alias reais do Figma, sem inventar nada. `info` não tem essa role e continua igual ao `filled`. Ver `packages/web/src/components/badge.tsx`, `packages/web/src/components/button.tsx` (variant `destructive`) e o preset (`destructive`/`success`/`warning.foreground` → `color-text-neutral-inverted`) pro caso concreto.
- CSS vars: `--nemo-color-<grupo>-<...>`, `--nemo-space-*`, `--nemo-radius-*`, `--nemo-font-family-{inter,owners-text,owners-narrow}`, `--nemo-font-size-0..10`.
- `build/rn/theme.{light,dark}.ts` inclui `space`/`radius`/`borderWidth`/`font` (+ primitivos de cor) além do alias de cor — não só cor como antes. Style Dictionary não converte dimensão pra número sozinho (fica `"16px"` string); o formato `nemo/rn-theme` em `build/build.mjs` faz isso na mão (`nest()`), mesmo parsing que o formato Dart já fazia. RN acessa via `t.space["100"]`, `t.radius.lg`, `t.font.size["4"]`, `t.font.weight["semi-bold"]` (`useNemoTheme()`, `theme.tsx`) — nomes de chave vêm direto do token (`"12"`, `"25"`, `"semi-bold"` etc.), não é um enum amigável. Flutter já tinha isso desde o início (`NemoTokens.space25`, `.radiusLg`, `.fontSize4` — nomes camelCase via `dartName()`), só não estava sendo consumido pelos 3 widgets portados (ver Paridade de plataforma).
- **Preset Tailwind** (`packages/web/tailwind.preset.js`) mapeia os papéis do shadcn → tokens reais: `bg-primary`→`interactive-accent-primary-main`, `bg-background`→`surface-neutral-primary`, `text-foreground`→`text-neutral-primary`, `border-border`→`border-neutral-main`, `bg-card`→`surface-neutral-tertiary`, feedback `destructive/success/warning`→`icon-semantic-*`, e `sidebar-*`. Dark via classe `.dark`.

## Convenção pra adicionar um componente (shadcn-style)
1. `packages/web/src/components/<nome>.tsx` — source canônico do shadcn (New-York). Únicas mudanças: `import { cn } from "../lib/utils"` (relativo), ícones de `lucide-react`, cross-imports relativos (`"./button"`). Manter as classes de papel do shadcn (o preset tematiza). **Sem** `"use client"`.
2. `<nome>.stories.tsx` — story com `tags: ["autodocs"]`, exemplos em pt-BR com contexto Daki (pedidos/entrega).
3. Exportar em `packages/web/src/index.ts` (`export * from "./components/<nome>"`).
4. Se a lib for nova, adicionar em `packages/web/package.json` + `npm install` + reiniciar o Storybook.
5. Verificar no Storybook (screenshot) em light **e** dark.
6. **Checar acessibilidade antes de considerar o componente pronto** — não é opcional nem só pra componente com texto pequeno ou cor "estranha", todo componente novo passa por isso:
   - **Contraste real, não no olho**: pra cada par cor-de-texto/cor-de-fundo novo (principalmente combinações que não são as classes de papel padrão do shadcn), calcular a razão WCAG de verdade — texto normal precisa de 4.5:1, texto grande (≥24px ou ≥18.66px bold) e objetos gráficos/UI (ícone, borda, indicador) precisam de 3:1. Não assumir que "parece legível" é suficiente; o caso do `NavigationBar` (`#001e6b` sobre `#0069ff` dava só ~3.2:1, e nem preto puro chegava a 4.5:1 nesse azul) só foi pego calculando.
   - **Foco visível de verdade**: testar com Tab de teclado real (clique de mouse não ativa `:focus-visible` na maioria dos browsers, então clicar não é teste válido). Prestar atenção especial quando o componente tem fundo colorido/customizado — `ring-ring` (ou qualquer token de anel padrão) pode coincidir com a própria cor de fundo do componente e ficar invisível; se o fundo for um literal fixo (não token que vira com o tema), o anel também precisa ser fixo, não um token que muda com o tema.
   - **Semântica**: `aria-current`/`aria-selected` pra estado ativo/selecionado, `aria-label` em containers de navegação (`<nav>`, etc.) sem texto visível, `aria-hidden` em ícone/decoração puramente visual, e texto `sr-only` pra qualquer informação que só existe como cor/ícone/badge (dot de notificação, contador) — sem isso a informação não existe pra quem usa leitor de tela.
   - Documentar no topo do arquivo do componente qualquer desvio da cor/valor literal do Figma feito por causa de acessibilidade (com o número real da razão de contraste), do mesmo jeito que já documentamos desvio de token ausente.

Fontes de marca (Owners + Inter) já em `packages/web/src/fonts/` + `@font-face` em `src/styles.css`; RN em `assets/fonts/` (`react-native.config.js`/expo), Flutter em `pubspec.yaml`.

## Status
- ✅ Pipeline de tokens (Figma-only) → web/RN/Flutter; Storybook com Foundations (Colors, Alias Colors, Typography, Radius & Spacing) + ~49 componentes shadcn tematizados (inclui Menubar, Form/react-hook-form).
- ✅ `KanbanCard`/`KanbanTaskCard` (Order/Stacking/Task, urgência) espelhado em web/RN/Flutter + Code Connect. `MenuItem`/`MenuSection`/`MenuList` (menu do app).
- ✅ Artifact publicado (showcase estático).
- ✅ Graphify instalado e grafo gerado (`graphify-out/`: `graph.html`, `GRAPH_REPORT.md`, `graph.json`). Rodar `graphify query "<pergunta>"` pra navegar o repo sem reconstruir; `graphify-out/.graphify_python` guarda o interpreter certo. Reindexar depois de mudanças grandes: `/graphify . --update`.
- ✅ `Badge` (o Tag/Chip unificado) estendido com `size` (sm/md), `shape` (pill/square), `type="solid"` e `count` — fecha a spec proposta em `componentes-comuns-daki-ds.html` pra substituir os ~15 tags nativos dos produtos. Segundo componente (depois do Kanban) a chegar nas 3 plataformas: espelhado em RN (`Badge.tsx`) e Flutter (`NemoBadge` em `nemo_badge.dart`). Ressalva: `type="solid"` só é visualmente distinto do `filled` pra `default`/`inverted`/`disabled` (via `Interactive/Accent/Primary`, aliased de verdade) e `success`/`warning`/`critical` (via o pin `-fixed`, mesmo padrão que `button.tsx` já usa pra `default`/`destructive`) — pra `info` renderiza igual ao `filled` de propósito, porque não tem um tom forte dedicado (nem `button.tsx` tem esse caso) e falta o token real (ver backlog item 6).
- ✅ `Toast` (`sonner.tsx`) tematizado: os variants nativos do sonner (`success`/`error`/`warning`/`info`/`loading`) usam o mesmo par "soft" que `Badge`'s `type="filled"` (`*-soft`/`*-soft-foreground` + borda semântica) em vez do `bg-popover` genérico; `loading` reusa `muted` por não ser uma cor semântica. Gotcha: as classes de cor de cada variant precisam de `!` (important) — sonner aplica o `classNames.toast` base e o `classNames[type]` no mesmo elemento, mesma especificidade, e a ordem de declaração das cores no `tailwind.preset.js` fazia `bg-popover` vencer o empate silenciosamente. Verificado em Storybook (light + dark).
  - Motion/posição fechados (eram decisões em aberto no `toast-design-guide-cenarios.html`, seção 7): `position="top-right"` fixo (sem hook — abaixo do breakpoint mobile do sonner, 600px, `top-right`/`top-center` renderizam idêntico, só o lado vertical importa); `TOAST_DURATION` exportado (`short`/`medium`/`long`/`persistent`, valores do Jake) substitui ms soltos por chamada; `visibleToasts={3}` explícito (bate com o default do sonner, mas pinado por decisão, não por acidente); `closeButton` sempre visível, estilizado neutro (não muda por variant, igual ao `closeIcon` fixo do Jake); `swipeDirections={["top","right"]}` explícito (sonner já infere isso sozinho da `position`, só documentei); dedupe automático por `id` derivado de `tipo:mensagem` em `success`/`error`/`warning`/`info`/`loading`/`message` (usa o replace-por-id nativo do sonner, sem inventar mecanismo — mesma ideia do dedupe texto+tipo do Jake). O aria-label do botão fechar ("Fechar") só foi possível com `sonner` na versão 2.x (`^2.0.0`, era `^1.7.0`) — a 1.x não expunha essa prop; conferido que `ToastClassnames`/a API do `toast` não mudaram entre as versões antes de fazer o bump. Gotcha do upgrade: a 2.x removeu todo `:where()` do CSS interno do sonner, dando especificidade real a seletores que antes eram zero — isso quebrou o peso do `title` e a cor da `description` silenciosamente (só pego reconferindo estilos computados, não só o diff). Corrigido tirando bg/border/text do `classNames.toast` de vez e redefinindo `--normal-bg`/`--normal-border`/`--normal-text` (que o próprio sonner lê) direto pros tokens Nemo em `styles.css` — evita duas classes `!important` nossas empatando na mesma propriedade (ordem de desempate do Tailwind nesse caso é ordem de descoberta do JIT, não confiável).
- ✅ `MenuShortcutItem`/`MenuShortcutList` (`menu-shortcut.tsx`) — atalhos da home ("Pedir novamente", "Favoritos"): chip circular + label de 2 linhas, vários lado a lado num scroller horizontal. Mesma convenção de cor do `MenuItem` (`surface-accent-primary`/`text-accent-primary`); portado do node do Figma `Daki App • Components — Design in Progress` (node 12027:190966) — esse arquivo usa nomes de variável (`Surface/Accent/Tertiary`, `Surface/Semantic/Positive`) que não existem no Alias oficial (`Design Tokens V3`), então mapeei pro token real mais próximo em vez de inventar um novo. Web only por enquanto (RN/Flutter ainda não, igual `MenuItem`).
- 🐛 Fix: `asChild` em `MenuItem`/`MenuShortcutItem` quebrava (Radix `Slot` recebia os `<span>`s internos como filho em vez do elemento único passado pelo consumidor — `React.Children.only` explodia). Corrigido computando o conteúdo interno (ícone+label) e, quando `asChild`, clonando o filho do consumidor com esse conteúdo dentro, antes de entregar pro `Slot`.
- ✅ `ProductCard` (`components/product-card/`) — HUBR "App • Product Card" (mesmo arquivo Figma do Kanban), primeiro passo de uma futura família de cards entre os 5 produtos Daki (o dicionário de componentes já apontava `Card` como "conceito onipresente, muitas variações contextuais" — só o HUBR tem ~20 componentes com "card" no nome). **Compound components (`data-slot`), mesmo idiom de `Item`/`Field`/`Empty`/`Card`** — não o padrão de props tipadas que o `KanbanCard` usa (esse é anterior a essa convenção pegar tração no repo): `ProductCard` (casca — só cuida de `overflow-clip`+`rounded-lg`+sombra), `ProductCardBody`, `ProductCardMedia`, `ProductCardTitle`, `ProductCardTags`, `ProductCardPill`, `ProductCardLocation`, `ProductCardText`, `ProductCardSeparator`, `ProductCardFooter`, `ProductCardStepper` — cada um só sabe de layout, nenhum entende "pedido"/"caixa"/"status de picking"; os 3 fluxos reais do HUBR (picking, pré-picking, bipagem de conferência) são construídos direto compondo esses subcomponentes, sem prop de negócio na API. `overflow-clip` na casca arredonda automaticamente qualquer filho que toque a borda (a faixa colorida do `ProductCardFooter` inclusive), então nenhum subcomponente precisa de `rounded-t-*`/`rounded-b-*` próprio. Espelhado em RN (`ProductCard.tsx`) e Flutter (`product_card.dart`), mesmos subcomponentes. Não migra o código real do HUBR (`ProductCard.tsx`/`card-scanning.tsx` do produto) — isso é trabalho futuro no repo do produto, mesmo padrão da migração de Badge/Toast (ver backlog).
  - Depois do rebase com o PR de paridade de token (#14), alinhado RN/Flutter ao mesmo padrão que `Badge`/`KanbanCard`/`KanbanTaskCard` já tinham recebido: `ProductCard.tsx` (RN) trocou os números soltos por `t.space`/`t.radius`/`t.font` e `product_card.dart` passou a usar `NemoTokens.spaceNN`/`fontSizeN` também nos espaçamentos que ainda estavam com valor cru. Achado no meio do caminho: título e stepper estavam 18px em RN/Flutter contra `text-lg` (20px) no web — mesmo drift que o PR #14 já tinha corrigido no `KanbanTaskCard` (web como fonte da verdade); corrigido subindo RN/Flutter pra 20px, não descendo o web. Também corrigido o raio de canto dos botões +/- do stepper em RN, que estava em 6 (não existe esse step na escala; o web usa `rounded-md`/8). Botões +/- do stepper (3 plataformas) ganharam rótulo de acessibilidade ("Diminuir/Aumentar quantidade"), mesma convenção que `ProductTile` (`FavoriteChip`/`RemoveButton`) já usa.
  - Refatorado (3 plataformas) depois do Figma trocar o node de referência pra 19188:22490 ("Product card", variants "Badge horizontal"/"Badge vertical") — esse node ficou boolean/variant-driven (cada seção liga/desliga por prop, `variant` só troca o layout do `topBadges`) em vez de um node por fluxo HUBR, e trocou o "Cód. `<value><highlight>`" formatado por um bloco de texto genérico ("Content"/"text-secondary"). Duas mudanças: (1) o antigo `ProductCardCode` (prefixo "Cód." fixo + split value/highlight, específico do fluxo de bipagem do HUBR) virou `ProductCardText` (`primary`/`secondary`, sem formatação hardcoded — quem ainda precisa do visual antigo aninha texto estilizado dentro de `primary`); (2) novo `ProductCardWithBadges`, camada fina que espelha o toggle do Figma (cada seção é uma prop opcional, presente/ausente = mostra/esconde) compondo os mesmos primitivos por baixo — não substitui a família composable original, que continua exportada e é a única forma de expressar fluxos fora desse node (o stepper de bipagem de conferência, por exemplo, não existe nesse node do Figma). `ProductCardTags` ganhou `layout="row"|"column"` pra cobrir a variante vertical. Motivação (usuária, 2026-07-30): os componentes anteriores estavam "atrelados ao contexto" (linguagem/HUBR-específica); o pedido foi mantê-los agnósticos como o Figma, sem perder a flexibilidade de composição livre — daí o meio-termo (primitivos genéricos + wrapper de conveniência) em vez de só copiar as props booleanas do Figma 1:1. Code Connect (`product-card.figma.tsx`) remapeado pros 2 novos node-ids (19188:22491/19189:69379) via `ProductCardWithBadges`; os 3 fluxos HUBR não têm Code Connect próprio (não são nodes distintos no Figma).
  - Stories (`product-card.stories.tsx`) reduzidas a só `Horizontal`/`Vertical` (as duas variantes do node 19188:22490, via `ProductCardWithBadges`) — os fluxos HUBR não têm mais story própria, só os subcomponentes exportados; pedido explícito da usuária pra limpar o ruído do Storybook. Descrição do componente no topo do arquivo (o que virava texto longo no Docs do Storybook) também enxugada pra 1-2 linhas.
  - Revisão pós-refactor (pedido da usuária: conferir reuso de Badge/divisor/fontes/tokens já existentes) — divisor e tokens já estavam ok (mesmo padrão hand-rolled do `KanbanCard`, sem valor cru); achado real: as tags do Figma têm ícone+bolinha+texto (a mesma anatomia do `Badge`/`NemoBadge`), mas o exemplo usava `ProductCardPill` só com ícone, sem bolinha — não dava pra usar o `Badge` de verdade porque nenhuma cor dele é o cinza neutro do Figma (só tons fortes de marca/semânticos). Corrigido dando ao `ProductCardPill` (3 plataformas) as mesmas props `icon`/`dot` do `Badge`/`NemoBadge` — cinza neutro, mas com a mesma anatomia — em vez de inventar uma variante de cor nova nele.
- ✅ **`packages/web/src/icons/`** — 72 ícones reais (não redesenhados) portados do Figma `icons-DakiApp` (node `33858:72174` do arquivo "Daki App • Components — Design in Progress"), exportados a 24px, um `export function Daki<Nome>Icon(props: React.SVGProps<SVGSVGElement>)` por ícone, agrupados por categoria: `general.tsx` (17: setas, chevrons, close, plus, etc.), `tabbar.tsx` (7), `delivery.tsx` (2), `delivery-statuses.tsx` (9), `toast-message.tsx` (8), `address.tsx` (3), `points.tsx` (3), `profile.tsx` (2), `order.tsx` (1), `coupon-wallet.tsx` (1), `payments.tsx` (19 logos de bandeira/carteira). Barrel em `icons/index.ts`, exportado no `index.ts` principal. Galeria de conferência em `foundations/icons.stories.tsx` ("Foundations/Icons").
  - Ícones monocromáticos (todas as categorias exceto Payments) usam `currentColor` — herdam a cor do texto, tema-áveis como os do `lucide-react`.
  - `delivery-statuses.tsx` é a exceção: são ícones semânticos de 2 cores (forma de fundo fixa `#172037` + glifo colorido por status — verde/vermelho/amarelo), cor **não** vira `currentColor` de propósito, porque a cor É o significado.
  - `payments.tsx`: logos de marca reais (Visa, Mastercard, PIX, Apple/Google Pay etc.) — cores/gradientes preservados exatamente como no Figma, **não** tema-áveis (exceto `DakiPaymentGenericCardIcon`, o ícone genérico de fallback, que usa `currentColor`). **Discover ficou de fora**: o asset do Figma pra esse veio como PNG raster já achatado, não vetor — precisa ser reexportado como vetor no Figma antes de portar.
  - Ressalvas pra revisar com o time: `DakiTabbarPlusIcon` (camada "plus" da Tabbar) na verdade é um glifo de sparkle/estrelas no Figma, não um `+` — portado fiel ao original. 5 ícones do General (`DakiChevronDownIcon`/`Up`/`Left`, `DakiCheckmarkIcon`, `DakiMinusIcon`, `DakiLocationIcon`) precisaram ter uma rotação calculada e "assada" no path, porque o Figma aplica a rotação fora do asset exportado — conferidos visualmente no Storybook, mas vale um segundo olhar.
  - Fora do namespace `icons-DakiApp/`, o mesmo arquivo do Figma também tem um conjunto maior e mais completo de ícones de status de entrega (`Delivery statuses / 24 / <Picked|On Route|Nearby|Arrived|Delivered> <Success|Warning|Fail|Pending>`, ~20 instâncias) que não segue a convenção `icons-DakiApp/` — não portado ainda; vale confirmar com o time se é esse (e não o `Delivery-statuses` menor já portado) o conjunto realmente usado no fluxo de rastreio do pedido.
- ✅ `AddToCartButton`/`CartCountBadge`/`FavoriteButton` (`add-to-cart.tsx`) — o componente "AddTo" do Figma (node `3872:52310`): botão de adicionar ao carrinho que vira um stepper de quantidade (lixeira em vez de "−" quando `quantity === 1`, já que decrementar de novo remove o item), badge somente-leitura "X N", e toggle de favorito (coração outline/preenchido). Controlado via props (`quantity`/`loading`/`disabled` + callbacks `onAdd`/`onIncrement`/`onDecrement`) — não guarda estado próprio, mesmo padrão do resto do design system. Reusa `DakiPlusIcon`/`DakiMinusIcon` do acervo de ícones (mesmo asset do Figma); o ícone de lixeira é novo, vive local ao arquivo (não é da lista `icons-DakiApp/General`, é específico deste componente); o coração usa `Heart` do `lucide-react` (glifo genérico, sem versão Daki-específica no Figma). O spinner de loading preserva a cor `#1759FF` fixa do Figma (não vira `currentColor`, já que é usado sobre fundo neutro, não sobre o azul). Verificado em Storybook (todos os estados + fluxo interativo real), light + dark.
- ✅ `ProductTile` (`components/product-tile/`) — o card de produto do Figma "Product Tile" (node `38835:30351`, 5 variantes): `layout="vertical"` (card de vitrine) e `layout="horizontal"` (linha de lista), cada um com estado `unavailable` (imagem `opacity-50` + tag "Volto logo" + `AddToCartButton` desabilitado), mais `type="orderDetail"` (linha somente-leitura de histórico/reembolso, com o badge flutuante "Reembolso • N un." e preço com multiplicador, ex. "2x R$ 17,09"). Reusa `AddToCartButton` de verdade (mesmo componente, mesmo comportamento) — não reimplementa o stepper. Layout vertical/horizontal-default têm o chip de favorito (coração pequeno sobre a imagem); horizontal-default tem um "x" de remover ao lado do nome em vez do coração; `orderDetail` não tem nenhum dos dois. Cores de texto (`text/accent/secondary`, `text/semantic/informative`, `text/semantic/negative` — vocabulário desse arquivo do Figma, não o Alias oficial) mapeadas pros tokens reais mais próximos (`text-accent-foreground`, `text-muted-foreground`, `text-destructive-soft-foreground`). Verificado em Storybook (as 3 stories, incluindo o fluxo interativo de favoritar/adicionar), light + dark.
- ✅ `NavigationBar` / `NavigationBarItem` / `NavigationBarBagItem` (`navigation-bar.tsx`) — a tabbar do app (node `40366:141533` do Figma "Daki App • Components — Design in Progress"): pill azul com `NavigationBarItem`s controlados (`active`/`dot`/`onClick`, sem estado próprio) e um `NavigationBarBagItem` cujo fundo escuro fixo só aparece com `count` — vazio (`count` ausente), vira visualmente um `NavigationBarItem` comum (`bg-primary`, ícone/label brancos); o destaque escuro é "tem item aqui", não uma decoração fixa do slot. Também aceita `active` — o Figma nunca mostrou esse slot "atual" (só o estado sempre-escuro com itens), então sem valor confirmado pra "mais escuro ainda" escurece o próprio `primary-active` via `brightness-90` (filtro relativo) em vez de chutar um hex novo; revisar quando o design tiver esse estado desenhado. Cor escura (tab ativa/fundo da Sacola/borda do badge) é `primary-active` (`interactive/accent/primary/active`, blue-10/`#001848`) — token real, não hex hardcoded (ver backlog item 7); precisou adicionar esse alias no preset (`tailwind.preset.js`), já existia no Alias mas não estava exposto como classe Tailwind. Todos os 6 ícones já existiam no acervo `icons-DakiApp/Tabbar` — confirmado por diff do path SVG real baixado do Figma, não só por nome (`DakiTabbarHomeIcon`/`CategoriesIcon`/`SearchIcon`/`OrdersIcon`/`BagIcon`; o ícone "Tabbar / User" do Figma tem o path idêntico ao já portado `DakiTabbarMenuIcon`, só reusado aqui). Fundo é `bg-primary` (`interactive/accent/primary/main`, `#0069ff`) — a variável ligada no Figma diz `surface/accent/primary`, mas esse role é um tint pálido no nosso Alias (`blue-95`); a cor renderizada bate com o brand blue, mesmo par que `Badge`'s `color="default" type="filled"` já usa. Largura dos itens é `flex-1` (o `w-[57.333px]` do Figma é só o cálculo daquele frame-amostra, não uma medida fixa pretendida). Margem externa do Figma (8px horizontal / 16px do fundo) é chrome de tela, não ficou no componente — cabe à tela que consome posicionar (`fixed inset-x-2 bottom-4` etc). Verificado em Storybook (Default = réplica exata do frame, Interactive = troca de aba real), light + dark. Nota pro time: o 5º item do Figma tem ícone de perfil mas o texto ainda diz "Início" (igual ao 1º item) — parece rótulo não atualizado no arquivo, reproduzido fiel; a story `Interactive` já usa "Perfil" como exemplo do rótulo provavelmente pretendido.
- ✅ `NavigationBar` portado pra RN (`NavigationBar.tsx`) e Flutter (`nemo_navigation_bar.dart`, `NemoNavigationBar` — prefixado pra não colidir com o `NavigationBar` nativo do Material). RN usa os 6 ícones reais (path SVG idêntico ao web, adicionados em `icons.tsx`: `TabbarHomeIcon`/`CategoriesIcon`/`SearchIcon`/`OrdersIcon`/`UserIcon`/`BagIcon`) — como RN não tem `currentColor`, `icon` virou render-prop (`icon={(color) => <TabbarHomeIcon color={color} />}`) em vez de elemento pronto, pra esse componente poder controlar a cor do estado `active` sem precisar de `cloneElement`. Flutter usa `Icons.*` do Material como aproximação (`Icons.home_outlined`, `Icons.grid_view`, `Icons.search`, `Icons.receipt_long_outlined`, `Icons.person_outline`, `Icons.shopping_bag_outlined`) — mesma decisão que `KanbanCard`/`ProductCard` já tomaram nesse package; os vetores reais existem (web/RN) se precisar de fidelidade exata depois, mas isso exigiria adicionar `flutter_svg` ao `pubspec.yaml` — sem esse pacote hoje, e sem toolchain Flutter neste ambiente pra validar visualmente, não dava pra transcrever as curvas Bézier na mão com confiança. Sombra de duas camadas do Figma não existe em RN/Flutter (só um `boxShadow`/`BoxShadow` simples, mesmo tratamento "não é token" do `nemoCardShadow`/`_nemoShadow`). Não verificado visualmente (sem toolchain RN/Flutter neste ambiente) — só revisão de código linha a linha contra os tokens gerados.
- ✅ **Auditoria retroativa de contraste WCAG** (2026-07-31, fechando parte do gap do backlog item 8) — em vez de reler os ~49 componentes shadcn um por um, resolvi os aliases reais (light/dark, `build/web/nemo*.css`) pros ~30 papéis do `tailwind.preset.js` e calculei a razão de contraste de verdade (fórmula WCAG) pra cada par texto/fundo e borda/fundo que o preset expõe — cobre a superfície inteira, já que os componentes shadcn só compõem esses papéis (não cor solta). Achados reais (não teóricos — cada um tem uso confirmado no código):
  - **Corrigido**: `border-input` (`Input`/`Textarea`/`SelectTrigger`/`InputOTP` — os únicos lugares onde a borda É o indicador de limite do componente) resolvia pro mesmo alias fraco do `border` decorativo (`border-neutral-main`, ~1.2:1 light / ~2.7:1 dark — falha o 3:1 do WCAG 1.4.11). `tailwind.preset.js` agora separa os dois papéis (shadcn já distinguia `border-input` de `border-border` no código, só o preset não): `input` passou a usar `border-neutral-hover`, alias real já existente, sem inventar hex — ~4.3:1 light / ~7.5:1 dark, passa em ambos os temas. `border` (decorativo — `Card`/`Sheet`/`Item`/`Separator`, que têm preenchimento/sombra como pista extra de limite, então não são obrigados ao 3:1) ficou como estava, de propósito. Verificado no Storybook (Input, Select, Card) light + dark.
  - **Documentado, não corrigido** (decisão de design, não bug de implementação — teria alcance amplo, mudaria o "peso" do texto secundário no sistema inteiro): `muted-foreground` (`color-text-neutral-tertiary`) só bate 3.21:1 sobre `bg-muted` (falha 4.5:1) e 4.27:1 sobre `bg-background` (falha por pouco) no tema claro — o achado mais concreto é a **label da aba inativa do `Tabs`**, que usa exatamente esse par e fica abaixo do mínimo de texto do WCAG. Fixar exigiria trocar o alias de `muted.foreground` (afeta ~34 arquivos que usam `text-muted-foreground`) ou dar um estilo local só pro `Tabs` — decisão de quem cuida do "tom" visual do sistema, não algo pra mudar sem validar com design. Novo item 9 no backlog.
  - **Já rastreado**: `Badge`/`Button` `type="solid"` `color="success"` mede 4.37:1 (falha por 0.13 o mínimo de 4.5:1) no tema claro — mesma causa-raiz do backlog item 6 (falta o role "On Success" dedicado no Alias do Figma); número real anexado lá.
  - Estados `disabled` (3.47:1/4.07:1) não contam como falha — WCAG 1.4.3/1.4.11 explicitamente isentam componentes inativos.
  - Script usado (resolve `var()` recursivamente + fórmula de luminância relativa real, não estimativa) virou ferramenta permanente do repo: `node build/contrast-audit.mjs` (roda contra `build/web/nemo*.css`, atualizar a lista `ROLES`/`TEXT_PAIRS`/`UI_PAIRS` se `tailwind.preset.js` ganhar papéis novos).
- ✅ **Onda 1 de paridade RN/Flutter** (2026-07-31) — fechou os 5 componentes Daki-específicos que só existiam em web (`MenuItem`/`MenuSection`/`MenuList`, `MenuShortcutItem`/`MenuShortcutList`, `AddToCartButton`/`CartCountBadge`/`FavoriteButton`, `ProductTile`, `Toast`) + 19 primitivos shadcn "Bucket A" (layout/estilo puro, sem Radix com comportamento real): `alert`, `aspect-ratio`, `attachment`, `avatar`, `bubble`, `button-group`, `card`, `collection-banner`, `empty`, `field`, `input-group`, `item`, `kbd`, `label`, `progress`, `separator`, `skeleton`, `spinner`, `typography`. Feito via 5 subagentes em paralelo (um por bloco de componentes relacionados) depois de mapear o ~63 restante do shadcn em 3 buckets por dificuldade de porte (A = direto; B = ~23 precisam de comportamento real tipo foco/overlay/gestos; C = ~17 sem equivalente mobile direto) — Bucket B/C ficaram de fora desta rodada por decisão explícita (ver backlog item 10). **Zero dependência nova** em RN ou Flutter nesta rodada.
  - **`asChild`/Radix `Slot` não foi portado** — é conceito Radix-only, não existe em RN/Flutter. `MenuItem`/`MenuShortcutItem` (as únicas duas fontes web que tinham essa prop, incluindo o fix de `React.Children.only` documentado acima) simplesmente não expõem `asChild` no RN/Flutter; o item é sempre um `Pressable`/`InkWell` próprio.
  - **Colisão de nome com widget Material nativo do Flutter** resolvida com o mesmo padrão já usado em `NemoBadge`/`NemoNavigationBar`: `Card`→`NemoCard` (`nemo_card.dart`), `AspectRatio`→`NemoAspectRatio` (`nemo_aspect_ratio.dart`), `Typography`→`NemoTypography` (`nemo_typography.dart`, colisão conceitual com `ThemeData.typography`), `MenuItem`/`MenuSection`/`MenuList`→`NemoMenuItem`/`NemoMenuSection`/`NemoMenuList`, `MenuShortcutItem`/`List`→`NemoMenuShortcutItem`/`List`, `AddToCartButton`/`CartCountBadge`/`FavoriteButton`→`NemoAddToCartButton`/`NemoCartCountBadge`/`NemoFavoriteButton` (prefixados por consistência de família, não por colisão real). Os outros 13 do Bucket A (`Avatar`, `Kbd`, `Label`, `Separator`, `Skeleton`, `Spinner`, `Progress`, `Bubble`, `ButtonGroup`, `CollectionBanner`, `Empty`, `Field`, `InputGroup`, `Item`, `Attachment`) e `ProductTile` não colidem — nome direto.
  - **`Toast` não é porte, é solução nova** (`Toast.tsx` em RN, `nemo_toast.dart` em Flutter) — `sonner` não existe fora do browser. Replica só a API observável do `sonner.tsx` web: `TOAST_DURATION` (mesmos valores `short/medium/long/persistent`), dedupe por `id` derivado de `tipo:mensagem` quando não informado, cores "soft" por variante (mesmo triplet `surface.semantic.*`/`text.semantic.*`/`border.semantic.*` que `Badge`'s `type="filled"` já usa, `error`→mapeado pro `critical` do Nemo), máx. 3 toasts visíveis, botão de fechar sempre visível. Sem lib nova (RN: Context+`Animated`; Flutter: `Overlay`+`AnimationController`, ambos built-in). Posição: topo full-width nas duas (não replicou `top-right` do web — não existe "lado direito" relevante numa tela estreita). **Sem swipe-to-dismiss** nesta rodada (gesto — escopo consciente, documentado no topo dos dois arquivos, candidato a refinar junto do Bucket B).
  - Gaps CSS-only sem equivalente direto, documentados inline nos arquivos: `mix-blend-darken` do `CollectionBanner` (thumbnails renderizam sem o blend), `focus-within:ring-2` do `InputGroup` (precisa de estado de foco compartilhado — fora de escopo, Bucket A não tem comportamento real), `data-[invalid=true]` cascade do `Field` (callers estilizam os filhos direto), `border-dashed` do `Empty` (no web nunca renderiza de verdade — Tailwind preflight zera a largura sem uma classe `border` explícita — reproduzido fiel, não é regressão).
  - Ícone novo: `ChevronRightIcon` adicionado em `packages/react-native/src/icons.tsx` (trailing padrão do `MenuItem`, não existia nenhum chevron-right no acervo RN ad-hoc). `AddToCart.tsx`/`ProductTile.tsx` (RN) ganharam ícones locais próprios (`TrashIcon`, `HeartIcon` via `react-native-svg`, paths copiados 1:1 do lucide `Heart`/ícone local do web) — Flutter usa `Icons.*` do Material como aproximação (mesma decisão já tomada no `NavigationBar`, sem `flutter_svg` no pubspec).
  - **RN verificado ao vivo** (não só revisão de código): montado um app Expo separado (`nemo-preview/`, fora do repo — copia `packages/react-native` + `build/rn/*.ts` pra dentro do projeto Expo em vez de linkar via `file:`, que exigiria configurar `watchFolders`/symlink no Metro) rodando via `expo start --web`, aberto no Browser pane. Kitchen-sink screen com os 24 componentes novos + interação real: Toast (as 6 variantes, cor "soft" por tipo, botão fechar), stepper do `AddToCartButton`/`ProductTile` (estado compartilhado, troca "−"↔lixeira em `quantity===1`), `FavoriteButton` (toggle preenchido/outline), `MenuItem` (badge+dot+chevron), `MenuShortcutItem` (chip circular+label 2 linhas) — tudo conferido por screenshot real, não só leitura de tokens. **Flutter continua sem verificação visual** (Flutter SDK não está instalado nesta máquina, só as CLI tools do Xcode sem Simulator) — só revisão de código linha a linha contra `nemo_tokens.dart`.

## Paridade de plataforma

Snapshot do que existe onde (vs. texto espalhado no Status acima). Atualizar a cada componente novo/portado.

| Componente | Web | RN | Flutter | Notas |
|---|---|---|---|---|
| Badge (Tag/Chip unificado) | ✅ | ✅ | ✅ | |
| KanbanCard / KanbanTaskCard | ✅ | ✅ | ✅ | |
| ProductCard | ✅ | ✅ | ✅ | |
| MenuItem / MenuSection / MenuList | ✅ | ✅ | ✅ | RN: `MenuItem.tsx`. Flutter: `nemo_menu_item.dart` (`NemoMenuItem`/`NemoMenuSection`/`NemoMenuList`, prefixado). Sem `asChild`/Slot nas duas (conceito Radix-only) |
| MenuShortcutItem / MenuShortcutList | ✅ | ✅ | ✅ | RN: `MenuShortcut.tsx`. Flutter: `nemo_menu_shortcut.dart` (`NemoMenuShortcutItem`/`List`). Mesmo motivo acima pro `asChild` |
| AddToCartButton / CartCountBadge / FavoriteButton | ✅ | ✅ | ✅ | RN: `AddToCart.tsx`. Flutter: `nemo_add_to_cart.dart` (`NemoAddToCartButton`/`NemoCartCountBadge`/`NemoFavoriteButton`). Spinner de loading com cor fixa `#1759FF` replicada nas 3 |
| ProductTile | ✅ | ✅ | ✅ | RN: `ProductTile.tsx`. Flutter: `product_tile.dart`. Reusa o `AddToCartButton` já portado (não reimplementa o stepper) |
| NavigationBar / NavigationBarItem / NavigationBarBagItem | ✅ | ✅ | ✅ | Ícones reais (path SVG exportado do Figma) em web e RN; Flutter usa `Icons.*` do Material como aproximação (sem `flutter_svg` no pubspec ainda) |
| Toast | ✅ (`sonner.tsx`) | ✅ (`Toast.tsx`) | ✅ (`nemo_toast.dart`) | RN/Flutter **não são porte** — solução nova (Context+Animated / Overlay+AnimationController), sem lib nova, replicando só a API observável do sonner (`TOAST_DURATION`, dedupe por `tipo:mensagem`, cores soft por variante). Sem swipe-to-dismiss ainda (documentado nos dois arquivos como escopo consciente) |
| Ícones `icons-DakiApp` (72) | ✅ | ⚠️ | ❌ | RN ganhou +1 (`ChevronRightIcon`, pro trailing padrão do `MenuItem`) mas ainda não é o acervo do Figma — continua sendo um punhado de ícones ad-hoc |
| Bucket A shadcn (19: alert, aspect-ratio, attachment, avatar, bubble, button-group, card, collection-banner, empty, field, input-group, item, kbd, label, progress, separator, skeleton, spinner, typography) | ✅ | ✅ | ✅ | Onda 1 (ver Status). Sem dependência nova nas duas plataformas. `Card`→`NemoCard`, `AspectRatio`→`NemoAspectRatio`, `Typography`→`NemoTypography` no Flutter (colisão com Material/tema) |
| Bucket B shadcn (23: accordion, alert-dialog, breadcrumb, button, checkbox, collapsible, dialog, drawer, dropdown-menu, input, input-otp, pagination, popover, radio-group, select, sheet, slider, switch, table, tabs, textarea, toggle, toggle-group) | ✅ | ❌ | ❌ | Precisa de comportamento real (foco/overlay/gestos) — RN vai precisar de `@react-native-community/slider` + `@react-native-picker/picker` (decidido, ver backlog item 10); Flutter cobre a maioria com widgets Material nativos, sem lib nova |
| Bucket C shadcn (17: calendar, carousel, chart, combobox, command, context-menu, data-table, date-picker, direction, form, hover-card, menubar, navigation-menu, resizable, scroll-area, sidebar, tooltip) | ✅ | ❌ | ❌ | Sem equivalente mobile direto (right-click, hover, drag-resize, grid denso etc.) — vira nota de design no backlog, não porte de código (decidido, ver backlog item 10) |

RN e Flutter consomem os mesmos tokens gerados (`build/rn/theme.{light,dark}.ts`, `build/flutter/nemo_tokens.dart`) — cor **e** espaçamento/radius/fontSize (ver "Como os tokens funcionam"). O que falta pra cada linha ❌/⚠️ é o *componente*, não o token.

## Próximos passos (backlog)
1. **Revisão de tokens por componente** — aproximar do produto Daki real: revisar tons de cinza (algum pode estar errado), ajustar *qual* token cada componente usa (não os valores). É ajuste isolado por componente.
2. **Componentes nativos/específicos da Daki** que não existem no shadcn — lista a preencher:
   - _(adicionar aqui os componentes conforme o time listar)_
3. **Migração dos ~15 tags legados dos produtos pro `Badge` unificado** (trabalho nos repos de cada produto, não no Nemo) — mapeamento já proposto em
   `Produtos daki • Projetos front/componentes-comuns-daki-ds.html`:
   - `super-daki-tag` (HUBR) → `color="default" type="solid"`
   - `status-tag` (daki-zeus, ×4 versões módulo) → `color="success|warning|critical"`
   - `DiscountTag` (Daki App/Web) → `color="critical" type="solid"`
   - `counter-tag`/`PickingAmountTags` (HUBR) → `count` sem `children`
   - `delivery_status_tag` → `color="success|warning"`
   - `medal_tag` → `color="default"`
   - `ModalityTag`, `TripCompensationTags` e demais variantes: mapear caso a caso ao migrar cada produto.
4. **Migração dos toasts legados dos produtos pro `Toast` (sonner.tsx) tematizado** (trabalho nos repos de cada produto, não no Nemo) — call-sites e esforço por stack mapeados em
   `Produtos daki • Projetos front/toast-dev-spec-migracao.html`. Decisões de design ainda em aberto (truncate, fila vs. empilhar, swipe, dark mode) em `toast-design-guide-cenarios.html`.
5. **Consistência de Modal/BottomSheet/Drawer** (stacking, swipe-to-dismiss, safe-area) — mapeado em `Produtos daki • Projetos front/eval-viewer-component-flow-mapping.html`; mais acoplado a navegação, custo maior de unificação.
6. **Falta o role "On Critical/On Success/On Warning/On Info" no Alias do Figma** — confirmado direto no arquivo [Design Tokens V3](https://www.figma.com/design/EoOw5gJH3q6IsCI9W1mtHd/%F0%9F%8E%A8-Design-Tokens-V3), página "1. Semantic groups" (node `4239:12502`, frame "Error Success Warning Info"): o role **existe e tem o valor certo já documentado** — `On Critical`=C-100/branco sobre C-40, `On Success`=SC-100/branco sobre SC-40, `On Warning`=W-100/branco sobre W-40, `On Info`=I-100/branco sobre I-40 (mesmos valores que já tinha achado no `Color Palette` bruto do `figma-export.tokens.json`). Só que essa página usa cor fixa nos swatches (confirmado via `get_variable_defs` — não tem variável vinculada), é documentação visual, não a fonte real. No set `Alias colors /Value` (o que `import-figma-tokens.mjs` de fato lê) esse role nunca foi promovido — só `On <Hue> Container` está lá. Na prática isso já não bloqueia nada — `Badge`'s `type="solid"` e `Button`'s `destructive` resolvem o contraste parenado o tom forte com `text-neutral-inverted` (ver "Como os tokens funcionam"), sem precisar desse role. Mas promovê-lo ainda deixaria a intenção mais explícita no código (`bg-critical text-on-critical` em vez de reaproveitar um alias neutro). É tarefa pequena pra quem tem acesso ao Tokens Studio: adicionar ~4 aliases (ex. `Text/Semantic/Critical Strong → {Critical.On Critical}` e equivalentes) e reexportar. **Número real medido na auditoria de contraste (item 8)**: `Badge`/`Button` `type="solid"` `color="success"` (`text-success-foreground` sobre `bg-success`) mede **4.37:1 no tema claro** — falha o mínimo de texto do WCAG (4.5:1) por 0.13, exatamente o tipo de furo que promover o role "On Success" resolveria de vez.
7. **Falta o role "Surface/Decorative/Surface-decorative-600" no Alias do Figma** — usado no node `40366:141533` (`NavigationBar`, tab ativa + fundo do slot "Sacola" + borda do badge de contagem), valor `#001e6b`. Não está em nenhum primitivo (`color.navy.*`/`color.blue.*`) nem em `Alias colors /Value` — gap do mesmo tipo do item 6. Resolvido *na prática* sem esperar o Figma: `navigation-bar.tsx` usa o alias real `interactive/accent/primary/active` (`primary-active` no preset, blue-10/`#001848`, mesmo valor nos dois temas) em vez do hex — mesma tonalidade, contraste igual ou melhor em todo lugar que testei (~3.6:1 vs ~3.2:1 contra `bg-primary`). Ainda vale confirmar com o time se `surface-decorative-600` é pra ser essa mesma cor (aí só formalizar o alias) ou uma cor própria diferente — se for diferente, `primary-active` fica como aproximação até lá.
8. **Auditoria contra Definition of Ready / Definition of Done da Daki** (páginas Notion "Definition of Ready" / "Definition of Done", tag `DoR`/`Acordos de trabalho`) — feita em 2026-07-31. Lado design/token/acessibilidade está bem alinhado (tokens semânticos, desvios sempre documentados com número real medido — ver itens 6/7 acima e o histórico de contraste do `NavigationBar`/`Badge`/`Button`). Gaps reais, todos do lado de processo de engenharia (nenhum é bloqueado por token/Figma):
   - **Qualidade de código** (maior gap): zero lint (`.eslintrc`/`eslint.config` não existe), zero script de typecheck no `package.json`, **zero testes** (nenhum `.test.`/`.spec.` no repo, nem unitário nem regressão visual) — nem web, nem RN, nem Flutter.
   - **Versionamento**: sem `.changeset`, sem `CHANGELOG`, todos os pacotes travados em `0.1.0`.
   - **Deploy/Publicação/Comunicação**: sem workflow de CI (`.github/workflows` não existe), sem pipeline de publicação manual documentado, sem canal de anúncio de versão.
   - **Acessibilidade retroativa** (DoR 6/7/8): o passe de contraste calculado/foco visível/aria só era prática obrigatória **a partir do `Badge` em diante** (ver convenção acima) — os ~49 componentes shadcn portados antes disso não tinham essa verificação registrada. ✅ **Parte de contraste fechada** — ver entrada de Status "Auditoria retroativa de contraste WCAG": em vez de auditoria arquivo-a-arquivo, resolvi contraste real pra todos os ~30 papéis de cor que os componentes shadcn compõem (cobre os 49 de uma vez, já que nenhum usa cor solta) — achou e corrigiu `border-input`, documentou `muted-foreground` (item 9) e anexou número real ao item 6. **Foco visível e semântica aria ainda não foram auditados retroativamente** (só verificados nos componentes novos) — falta real.
   - **Estados de interação padronizados** (DoR 2): hover/focus/active/disabled vêm de graça via Radix, mas não há contrato formal pra `loading`/`skeleton`/`positive`/`negative` — cada componente resolve ad hoc.
9. **Decisão de design: `muted-foreground` não bate contraste de texto em todos os fundos onde é usado** — achado na auditoria de contraste do item 8. `muted.foreground` (`color-text-neutral-tertiary`) mede, no tema claro, **3.21:1 sobre `bg-muted`** (uso real: label da aba inativa do `Tabs`, `Kbd`, botão de cancelar do `Toast`, hover do `Toggle`) e **4.27:1 sobre `bg-background`** (uso real: descrições/legendas em ~34 arquivos) — os dois abaixo do mínimo de 4.5:1 pra texto do WCAG 1.4.3. No tema escuro passa em ambos (7.45:1/8.19:1). Não corrigido nesta rodada porque a única forma de resolver sem hex novo é escurecer o alias `muted.foreground` no preset, o que muda o "peso" visual do texto secundário/terciário no sistema inteiro (~34 arquivos) — decisão de quem cuida da linguagem visual, não um bug isolado de implementação. Se for pra frente: `text-neutral-secondary` (mais escuro, já existe) resolve os dois casos com folga (~8.9:1/~4.x:1 — conferir o par com `bg-muted` antes de trocar); alternativa mais cirúrgica é só sobrescrever a cor do label inativo dentro do `Tabs` (menor raio de impacto, mas não resolve os outros usos).
   - Consistente com a nota do topo deste arquivo: este clone é intencionalmente um snapshot cru (sem `.husky`/`commitlint`/`.changeset`/CI) — o gap era esperado, agora está mapeado.
10. **Paridade RN/Flutter — Bucket B e C do shadcn** (restante depois da Onda 1, ver Status e tabela de Paridade) — próxima(s) sessão(ões), em blocos curtos:
    - **Bucket B (23 componentes com comportamento real)**: `accordion`, `alert-dialog`, `breadcrumb`, `button`, `checkbox`, `collapsible`, `dialog`, `drawer`, `dropdown-menu`, `input`, `input-otp`, `pagination`, `popover`, `radio-group`, `select`, `sheet`, `slider`, `switch`, `table`, `tabs`, `textarea`, `toggle`, `toggle-group`. Decisão já tomada sobre dependências RN (Flutter cobre a maioria com Material nativo, sem lib nova): `@react-native-community/slider` pro `Slider`, `@react-native-picker/picker` pro `Select`. Overlays (`Dialog`/`Sheet`/`Drawer`/`Popover`/`DropdownMenu`) dá pra fazer só com o `Modal` nativo do RN (sem lib de portal/gesto nova) — confirmar caso a caso quando chegar a vez, não assumir que todos vão precisar de dependência.
    - **Bucket C (17 sem equivalente mobile direto)**: `calendar`, `carousel`, `chart`, `combobox`, `command`, `context-menu`, `data-table`, `date-picker`, `direction`, `form`, `hover-card`, `menubar`, `navigation-menu`, `resizable`, `scroll-area`, `sidebar`, `tooltip`. Decisão: **nota de design, não porte de código** — cada um precisa de uma decisão de produto sobre qual é o padrão mobile equivalente antes de virar componente (ex.: `Calendar`/`DatePicker` provavelmente viram o date-picker nativo do SO em vez de um grid customizado; `Tooltip` pode nem fazer sentido em touch sem um gatilho de long-press; `ContextMenu` idem (não há right-click); `Command`/`Combobox` são um padrão desktop de typeahead; `DataTable`/`Sidebar`/`Menubar`/`NavigationMenu` são padrões de app desktop; `Chart` precisa de uma lib de gráfico nativa própria por plataforma; `Carousel`/`ScrollArea`/`Resizable` têm equivalente parcial via scroll/paging nativo, mas não 1:1). Escrever essa nota componente a componente quando o time for revisitar, em vez de portar às cegas.

> Trabalhar em **sessões curtas por bloco** (contexto enxuto): cada componente/ajuste é independente e verificável no Storybook.
