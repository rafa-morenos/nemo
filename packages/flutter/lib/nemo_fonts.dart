/// Nemo brand font families (declared in pubspec.yaml → flutter/fonts).
/// Owners Narrow = display · Owners Text = headings · Inter = body/UI.
class NemoFonts {
  NemoFonts._();

  static const String display = 'Owners Narrow'; // weight 900
  static const String heading = 'Owners Text'; //   weight 400 / 500
  static const String sans = 'Inter'; //            body / UI
}
