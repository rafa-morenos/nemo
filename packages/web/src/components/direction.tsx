/**
 * Re-export of Radix's `DirectionProvider`.
 *
 * Wrap the app (or a subtree) in `<DirectionProvider dir="rtl" | "ltr">` to
 * make reading direction available to all Radix primitives (menus, sliders,
 * etc.) so they mirror correctly for RTL/LTR locales.
 */
export { DirectionProvider } from "@radix-ui/react-direction";
