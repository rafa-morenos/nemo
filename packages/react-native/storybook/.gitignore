node_modules/
.expo/
dist/
# `expo prebuild`/`expo run:android`/`expo run:ios` output — regenerated on demand,
# not committed (android/ alone is ~800MB with Gradle caches, ios/ ~900MB with Pods).
android/
ios/
# Regenerated by the storybook metro plugin from the stories glob on every run.
.rnstorybook/storybook.requires.ts
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/

# macOS
.DS_Store
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli